{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && cd ../frontend && npm install && npm run build && cd .. && python3 -m venv AI/LLM/.venv && AI/LLM/.venv/bin/pip install --no-cache-dir --upgrade pip && AI/LLM/.venv/bin/pip install --no-cache-dir -r AI/LLM/requirements.txt"
  },
  "deploy": {
    "startCommand": "bash ./start.sh",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
